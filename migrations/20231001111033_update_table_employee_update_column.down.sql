ALTER TABLE `employee` CHANGE `bidang_id` `bidang` VARCHAR(100) NOT NULL;

ALTER TABLE `employee` CHANGE `seksi_id` `seksi` VARCHAR(100) NOT NULL;

ALTER TABLE `employee` CHANGE `uk_id` `unit_kerja` VARCHAR(100) NOT NULL;
